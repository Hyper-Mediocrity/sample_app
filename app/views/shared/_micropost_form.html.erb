<%= form_for(@micropost) do |f| %>
  <div class="field">
    <%= render 'shared/error_messages', :object => f.object %>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
  
<% end %>